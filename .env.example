DATABASE_URL="file:./dev.db"
SESSION_SECRET="at-least-32-random-characters-here"

# Natural-language entry ("dinner 1800 with Rahul on card"). Optional — without a key,
# a built-in rule parser handles the common sentence shapes. Tried in this order:
GROQ_API_KEY=""        # openai/gpt-oss-20b — free tier covers a group of friends
OPENAI_API_KEY=""      # gpt-6-luna — ~$0.15 per 1,000 entries, good paid fallback
GEMINI_API_KEY=""      # gemini-2.5-flash-lite — free tier (Google may train on free-tier data)

# Optional
TELEGRAM_BOT_TOKEN=""
TELEGRAM_BOT_USERNAME=""
FAIRSHARE_API_KEY=""   # legacy shared key for the original Siri shortcut
